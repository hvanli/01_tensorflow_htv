# Bulldozer Price Prediction

This project predicts the sale price of bulldozers based on historical auction data.  
It includes data cleaning, feature engineering, model training, and evaluation using modern ML techniques.

---

## 📦 Project Setup

### 1. Clone the repository
```bash
git clone https://github.com/hvanli/bulldozer-price-prediction-2.git
cd bulldozer-price-prediction-2


Make sure you have environment.yml in the root of the project.
conda env create -f environment.yml
conda activate ml
python -m ipykernel install --user --name ml --display-name "Python (ml)"
